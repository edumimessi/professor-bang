name: professor_bang
description: MVP de professor de reforço com IA pedagógica adaptativa, acolhedora e segura.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  sqflite: ^2.3.3+1
  path: ^1.9.0
  provider: ^6.1.2

flutter:
  uses-material-design: true
